# Core number-card artwork

`card_red_0.png` through `card_yellow_9.png` are the 40 approved gameplay
card faces. Each source file is rendered directly at its original 2:3 aspect
ratio (1024 × 1536): no crop, recomposition, or replacement frame is applied.
